# Model manifests

The repository contains an immutable Parakeet model manifest and no model
bytes. The manifest records source commits, required artifact paths, checksums,
and attribution. Model license approval remains a release gate.
