# Security

Do not send secrets, personal data, audio files, model files, or production
logs in an issue or support request.

Syrinx keeps the default listener on `127.0.0.1`, requires authentication when
configured, rejects non-loopback binding, and redacts local paths from public
diagnostics.

The security contact and vulnerability response process are not selected. They
are release gates before public publication.
